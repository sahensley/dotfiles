- defaults:
    link:
      if: '[ -n "${WSL2}" ] && [ -f /etc/fedora-release ]'
      relink: true

- clean: ['~']

- link:
    ~/.inputrc:
      path: readline/dot_inputrc
    ~/.tmux.conf:
      path: tmux/dot_tmux.conf
    ~/.ideavimrc:
      path: jetbrains/dot_ideavimrc
